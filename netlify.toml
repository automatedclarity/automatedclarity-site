# netlify.toml — ACX Console

[build]
  command = ""
  publish = "public"

[functions]
  directory = "functions"
  node_bundler = "esbuild"

# Canonical ACX Matrix entry (root only)
[[redirects]]
  from = "/"
  to   = "/.netlify/functions/gate-matrix"
  status = 200
  force = true
